# EconDataForge -- Data Integration Module Dependencies
# Install all:  pip install -r requirements.txt

# === Core: Excel parsing ===
openpyxl>=3.1.2

# === Document parsing ===
python-docx>=0.8.11
python-pptx>=0.6.21

# === PDF parsing ===
pdfplumber>=0.10.0
camelot-py[cv]>=0.11.0

# === Numeric / image processing ===
numpy<2.0
opencv-python>=4.5.0
Pillow>=9.0.0

# === OCR (Pipeline E: scanned PDF) ===
paddleocr==2.7.0.3
paddlepaddle==2.6.2

# === Spatial grid (GeoCube) ===
h3>=4.1.0

# === LLM integration (M1 requirement understanding) ===
dashscope>=1.14.0

# === API server ===
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# === PDF rendering (scanned PDF generation / page-to-image) ===
PyMuPDF>=1.23.0

# === v2 可选后端(按需安装; 任一可用即可开启 structure_v2, 否则自动回退规则管线) ===
# docling>=2.0        # pip install docling   (PDF→结构; 需 HF 模型缓存可达)
# mineru>=3.0         # pip install mineru    (扫描件/公式专项, CLI)
# 默认后端: vlm_ocr(DashScope qwen-vl*, 复用 dashscope 依赖, 无需额外安装)
