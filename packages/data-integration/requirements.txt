# EconDataForge -- Data Integration Module Dependencies

# === Required (core pipeline) ===
openpyxl>=3.1.2

# === Optional: Document parsing ===
# python-docx>=0.8.11    # Word .docx parsing
# python-pptx>=0.6.21    # PowerPoint .pptx parsing

# === Optional: PDF parsing ===
# pdfplumber>=0.10.0     # PDF text-based table extraction
# pypdf>=4.0.0           # PDF processing backend
# camelot-py[cv]>=0.11.0 # PDF vector table extraction

# === Optional: Numeric / fuzzy matching ===
# numpy>=1.24.0          # Chart reverse engineering, OCR
# rapidfuzz>=3.0.0       # Fuzzy string matching for schema alignment

# === Optional: OCR (Pipeline E) ===
# paddleocr>=2.0.0       # Scanned PDF OCR
# opencv-python>=4.5.0   # Image processing for chart/OCR

# === Optional: Spatial grid ===
# h3>=4.1.0              # Uber H3 hexagonal grid for GeoCube

# === Optional: LLM integration (M1 requirement understanding) ===
# dashscope>=1.14.0      # Alibaba Cloud Qwen API (DASHSCOPE_API_KEY env var)

# Install all:  pip install -r requirements.txt
# Install core: pip install openpyxl
# Install docs: pip install python-docx python-pptx
# Install PDF:  pip install pdfplumber camelot-py[cv]
