# m09 方法卡 | RDD 断点回归

## 适用场景
存在门槛(资助金额阈值/绩效分数线)

## 数据要求
运行变量 + 阈值;局部多期带宽

## 识别假设
局部随机性;检验 covariate 连续性

## 代码模板
```python
# rdrobust(Y, runvar, c=threshold);报告 MSE 最优带宽 + 稳健 CI
```

## 输出解读规范
效应是 local;带宽敏感 → 必须报 2~3 带宽

## 湖北数据可适用性预判
❌ 未确认门槛变量 → 暂缓

## 护栏联动
- estimates 必须含 coef/se/ci_95/p/n/effect_size
- N<30 → 配 m12 Bootstrap;显著结论须 ≥2 稳健性检验
