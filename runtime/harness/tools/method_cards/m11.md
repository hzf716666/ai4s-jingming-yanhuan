# m11 方法卡 | 分解指数(份额/结构分解)

## 适用场景
错配类假设天然方法:总差异 = 结构效应 + 强度效应

## 数据要求
两期或多组的结构占比 + 强度(如 专利占比=集群占比×密度)

## 识别假设
算术恒等分解,无因果;用作描述统计的正式化

## 代码模板
```python
def decompose(share_s, share_c, density):
    # total_gap = Σ share_c × (density_i - mean density)
    return {'structure':..., 'intensity':..., 'total':...}
```

## 输出解读规范
结构/强度贡献可加总;说明错配中多少来自布局、多少来自密度

## 湖北数据可适用性预判
✅ H-A1 天然:89% 专利 vs 61.4% 集群 → 分解为 布局效应 vs 密度效应

## 护栏联动
- estimates 必须含 coef/se/ci_95/p/n/effect_size
- N<30 → 配 m12 Bootstrap;显著结论须 ≥2 稳健性检验
