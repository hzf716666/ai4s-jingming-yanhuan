# m12 方法卡 | Bootstrap 置信区间/小样本校正

## 适用场景
小样本(N<30)检验、相关 CI 的稳健替代;所有 m01-m04 的配套

## 数据要求
原样本即可

## 识别假设
同为观测;只改推断方法

## 代码模板
```python
import numpy as np
boot = [f(x_boot) for _ in range(2000)]
lo, hi = np.percentile(boot, [2.5, 97.5])  # BCa 更优
```

## 输出解读规范
CI 不跨 0 才可称显著;BCa 修正偏差

## 湖北数据可适用性预判
✅ H-A1 三都市圈 N=3 → 必须 Bootstrap;配 m03

## 护栏联动
- estimates 必须含 coef/se/ci_95/p/n/effect_size
- N<30 → 配 m12 Bootstrap;显著结论须 ≥2 稳健性检验
