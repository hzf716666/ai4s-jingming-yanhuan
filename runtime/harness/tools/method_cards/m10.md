# m10 方法卡 | 合成控制

## 适用场景
单个处理单元 + 多期面板(国家/省份级)

## 数据要求
处理前 ≥10 期,对照池大且可解释

## 识别假设
未处理单元线性组合模拟反事实;预训练期拟合优度高

## 代码模板
```python
from synthdid import SynthDID  # 或 R synth 等价实现
```

## 输出解读规范
RMSPE 预检 + 安慰剂(permutation)分布

## 湖北数据可适用性预判
❌ 面板期数不足(2 期)→ 暂缓

## 护栏联动
- estimates 必须含 coef/se/ci_95/p/n/effect_size
- N<30 → 配 m12 Bootstrap;显著结论须 ≥2 稳健性检验
