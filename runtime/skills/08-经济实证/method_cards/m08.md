# m08 方法卡 | PSM 倾向得分匹配

## 适用场景
处理组/对照组不平衡时匹配;资助特征→匹配 可用于异质性

## 数据要求
协变量 + 处理标签;样本 ≥100 更稳

## 识别假设
选择可观测;遗漏变量仍是威胁 → 与 regression 结合

## 代码模板
```python
from causalinference import CausalModel
# 或 sklearn.LogisticRegression 拟合 pscores → 1:1 最近邻 + caliper
```

## 输出解读规范
报告平衡性检查(标准偏差<0.1),匹配后 ATT + CI

## 湖北数据可适用性预判
⚠️ 65 集群样本偏小;可做但 ATT 精度受限

## 护栏联动
- estimates 必须含 coef/se/ci_95/p/n/effect_size
- N<30 → 配 m12 Bootstrap;显著结论须 ≥2 稳健性检验
