# OS
.DS_Store
*.swp
*.swo
*~
Thumbs.db

# Editors
.vscode/
.idea/

# Local npm config
.npmrc

# Node
node_modules/
dist/
dist-ssr/
*.local
.vite/
.turbo/
*.tsbuildinfo
coverage/

# Rust / Tauri
target/
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/

# Logs & caches
*.log
.cache/
.eslintcache
.superpowers/
.worktrees/
docs/superpowers/

# Local runtime & secrets (never commit)
.jingming-yanhuan/
secrets/
*.key
.env
.env.*

# Data integration pipeline output & runtime data
packages/data-integration/output/
packages/data-integration/output_test/
packages/data-integration/server_data/
packages/data-integration/models/

# Local embedding cache (rebuild via impact_corpus.py embed --batch 64, ~4h)
packages/data-integration/data/impact_corpus/impact_embeddings.npy
packages/data-integration/data/impact_corpus/embed_log.txt
packages/data-integration/data/impact_corpus/fetch_log.txt
packages/data-integration/data/impact_corpus/eval_p1*.log
packages/data-integration/data/impact_corpus/raw/

# Office temp lock files
~$*

# Build artifacts / installers
*.dmg
*.app
*.exe
*.msi

# Bundled sidecar binaries (fetched per-platform; too large for git)
apps/desktop/src-tauri/binaries/

# Third-party skill packs (fetched by scripts/dev/fetch-skills.sh)
runtime/skills/external/

# Bundled goal plugin (fetched by scripts/dev/fetch-goal-plugin.sh)
runtime/goal-plugin/

# Local temporary files
pasted.txt
run_jingming.bat

# Local demo recordings (too large for git)
videos/

# Local video-production tooling (fetched hyperframes skill packs)
.agents/
skills-lock.json

# Local runtime installs (OpenCode / Qoder sidecars, downloaded per-machine)
runtime/opencode-profile/
runtime/qoder-sidecar/

# Per-project research archives (each is its own git repo with own history)
projects-live/

# Local API keys / model weights / huggingface caches (per-machine)
/models/
/bge-m3/
